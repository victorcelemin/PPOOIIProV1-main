info:
  name: get by placa
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/vehiculos/placa/AAA000
  headers:
    - name: X-API-Key
      value: "123456789"
  auth:
    type: bearer
    token: "{{mi_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
