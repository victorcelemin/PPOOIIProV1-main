info:
  name: get car will be expired
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/public/vehiculos/documentos-por-vencer?dias=10
  params:
    - name: dias
      value: "10"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
