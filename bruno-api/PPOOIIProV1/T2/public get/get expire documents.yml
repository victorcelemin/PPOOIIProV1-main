info:
  name: get expire documents
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/public/vehiculos/documentos-vencidos

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
