info:
  name: zzz
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/
  headers:
    - name: "{{headers-api}}"
      value: "{{user-apiKey}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
